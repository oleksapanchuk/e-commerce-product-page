import style from './carousel.module.scss';

const Carousel = () => {
    return (
        <div className={style.container}>
            <div>Carousel</div>
        </div>
    );
};

export default Carousel;
